// This file is kept for backward compatibility
// We are now using MySQL backend instead of Firebase

// Create mock objects to prevent errors
const auth = {};
const db = {};
const storage = {};
const googleProvider = {};

export { auth, db, storage, googleProvider };
